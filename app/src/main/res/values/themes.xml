<resources>
    <!-- App theme -->
    <style name="Theme.Reciping" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
